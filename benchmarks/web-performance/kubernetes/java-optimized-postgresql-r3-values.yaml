# Apply after java-optimized-postgresql-values.yaml for the three-replica case.
fullnameOverride: bluemap-perf-java-new-postgresql-r3

replicaCount: 3

podLabels:
  bluemap.guenter.cloud/experiment-id: java-new-postgresql-r3

deploymentLabels:
  bluemap.guenter.cloud/experiment-id: java-new-postgresql-r3

storage:
  sql:
    maxConnections: 4
