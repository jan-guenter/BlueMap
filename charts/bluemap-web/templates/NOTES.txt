BlueMap Web is available inside the cluster at:
  http://{{ include "bluemap-web.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}

Health endpoints:
  /health/live
  /health/ready
{{- if or .Values.autoscaling.enabled (gt (int .Values.replicaCount) 1) }}

The web deployment uses external SQL storage and pod-local web roots, so
requests can be distributed without session affinity.
{{- end }}
{{- if include "bluemap-web.metricsEnabled" . }}

OpenMetrics is available inside the cluster at:
  http://{{ include "bluemap-web.metricsServiceName" . }}.{{ .Release.Namespace }}.svc:{{ .Values.metrics.port }}/metrics
{{- end }}
{{- if .Values.autoscaling.enabled }}

The HPA targets {{ .Values.autoscaling.utilizationMetricName }} at
{{ .Values.autoscaling.targetAverageConnectionUtilizationPercentage }}% average
connection-slot utilization. Verify that your custom-metrics adapter publishes
one value for every Ready BlueMap web pod.
{{- end }}
