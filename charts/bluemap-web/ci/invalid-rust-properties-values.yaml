webserver:
  implementation: rust

storage:
  type: sql
  sql:
    databaseType: postgresql
    host: postgresql.database.svc
    port: 5432
    properties:
      applicationName: bluemap
    credentials:
      existingSecret: bluemap-database
